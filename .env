# Real MySQL database on premium281.web-hosting.com (prosdfwo cPanel account).
# Host is "localhost" not the public server hostname: the Node app and MySQL run on
# the same physical box under shared hosting, so the app should always talk to MySQL
# over the local socket/loopback (localhost), not go out over the network to its own
# server's public hostname - that path isn't guaranteed open and is unnecessary here.
# Still update WEB_ORIGIN / PUBLIC_VERIFY_BASE_URL below once you know your real domain.

DATABASE_URL="mysql://prosdfwo__dgst_grants:DGSTGrants%402026@localhost:3306/prosdfwo__dgst_grants"

PORT=4000
NODE_ENV=production

JWT_ACCESS_SECRET="454f9acf1b907f8cbc835c1a72014ad6eccf8fd634b28a41eae834af40953ff1"
JWT_REFRESH_SECRET="f05682cef02c14b71a1bb311db0c3f320c559c02ba6fe6787e398c4779762600"
JWT_ACCESS_TTL="15m"
JWT_REFRESH_TTL="7d"

STORAGE_ROOT="./storage"

WEB_ORIGIN="https://dgst.prosensia.pk"
PUBLIC_VERIFY_BASE_URL="https://dgst.prosensia.pk/verify"
